(executables
 ((names (optcomp))
  (link_flags (-linkall))
  (preprocess no_preprocessing)
  (libraries (compiler-libs.common ppx_optcomp))))

(install
 ((section bin)
  (files ((optcomp.exe as ppx-optcomp)))))
